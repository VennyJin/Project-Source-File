initialize ajile
initialize camera

open up camera

generate image

project sequence

//first image

while(next_frame==1)
    if (project successfully)
        save_handle=1
            if (save_handle == 1)
                capture image
                next_frame=1
            end
    else
    next_frame =0
end